# Tuning history

Store one dated Markdown or YAML record per calibration change. Each record should identify the calibration revision, supporting logs, reason for the change, expected result, and observed result.
